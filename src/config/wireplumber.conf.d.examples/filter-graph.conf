node.filter-graph.rules = [
  ## The list of filter graph rules

  ## This rule example creates two filter graphs for each audio source node,
  ## but only when a node named "my-node" exists in the graph.
  # {
  #   matches = [
  #     {
  #       ## This matches all audio source nodes
  #       media.class = "Audio/Source"
  #     }
  #   ]
  #
  #   ## Conditions are optional. All entries are AND-ed: every condition must
  #   ## be satisfied for the actions to be applied. Within each condition,
  #   ## the matches entries are OR-ed: any one matching global object satisfies it.
  #   conditions = [
  #     {
  #       matches = [
  #         {
  #           node.name = "my-node"
  #         }
  #       ]
  #     }
  #   ]
  #
  #   actions = {
  #     create-filter-graph = [
  #       ## Multiple filter graphs can be defined here.
  #       ## The syntax is the same as the pipewire filter-chain conf files.
  #
  #       ## This is an example of a builtin passthrough filter
  #       {
  #         nodes = [
  #           {
  #             type = builtin
  #             label = copy
  #             name = passthrough
  #           }
  #         ]
  #       }
  #
  #       ## This is an example of a LADSPA rnnoise filter
  #       {
  #         nodes = [
  #           {
  #             type = ladspa
  #             name = rnnoise
  #             plugin = librnnoise_ladspa
  #             label = noise_suppressor_stereo
  #           }
  #         ]
  #       }
  #     ]
  #   }
  # }
]

